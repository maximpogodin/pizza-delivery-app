<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.delivery_society"
        tools:targetApi="31">
        <activity
            android:name="com.example.delivery_society.view.ClientActivity"
            android:exported="true"
            android:label="ClientActivity"
            android:theme="@style/Theme.delivery_society"></activity>
        <activity
            android:name="com.example.delivery_society.view.EmployeeActivity"
            android:exported="true"
            android:label="EmployeeActivity"
            android:theme="@style/Theme.delivery_society"></activity>
        <activity
            android:name="com.example.delivery_society.view.RegistrationActivity"
            android:exported="true"
            android:label="RegistrationActivity"
            android:theme="@style/Theme.delivery_society"></activity>
        <activity
            android:name="com.example.delivery_society.view.AuthorizationActivity"
            android:exported="true"
            android:label="Delivery Society"
            android:theme="@style/Theme.delivery_society">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <uses-library android:name="org.apache.http.legacy" android:required="false" />
    </application>

</manifest>